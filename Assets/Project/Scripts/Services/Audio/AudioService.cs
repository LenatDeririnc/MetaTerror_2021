namespace Services.Audio
{
    public class AudioService : IService
    {
        public void RegisterService()
        {
            // AudioSource source = AudioSourceContainer.Source(0);
            // source.loop = true;
            // GlobalAudioClipContainer.Clip(0).Play(0);
        }
    }
}